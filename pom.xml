<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.charles.eakins</groupId>
    <artifactId>zello-aggregator</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <!-- The single dynamic packaging tag is now at the bottom of the file -->

    <name>Zello Message Vault Aggregator</name>
    <description>A Spring Boot application to view Zello message history.</description>

    <properties>
        <java.version>24</java.version>
        <!-- FIX #1: Define the default packaging type as a property -->
        <packaging.type>jar</packaging.type>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-tomcat</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>executable-jar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <!-- No special properties needed, it will use the default 'jar' -->
        </profile>

        <profile>
            <id>deployable-war</id>
            <properties>
                <!-- This property will OVERRIDE the default when this profile is active -->
                <packaging.type>war</packaging.type>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
        <finalName>zello-aggregator</finalName>
    </build>

    <!-- FIX #2: The single, dynamic packaging tag that reads the property -->
    <packaging>${packaging.type}</packaging>

</project>